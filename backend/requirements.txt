# Versões escolhidas por já distribuírem wheels para CPython 3.14 — versões
# anteriores tentam compilar pydantic-core em Rust e exigem o compilador MSVC.
fastapi==0.141.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.51
alembic==1.14.1
# Driver do Postgres. O binário evita precisar de compilador na imagem.
psycopg[binary]==3.3.4
pydantic==2.13.4
pydantic-settings==2.7.0
openpyxl==3.1.5
# Necessário para o upload de arquivo na importação de extrato.
python-multipart==0.0.20
# Autenticação: hash de senha e tokens.
bcrypt==4.2.1
pyjwt==2.10.1
# Requerido pelo EmailStr do Pydantic.
email-validator==2.2.0

# Desenvolvimento
pytest==8.3.4
httpx==0.28.1
